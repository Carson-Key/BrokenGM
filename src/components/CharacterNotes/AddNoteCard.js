// UI
import Card from '../../ui/Card'
import CardTitle from '../../ui/CardTitle'

const AddNoteCard = () => {

    return (
        <Card className="h-80">
            <CardTitle>
                Add New Note
            </CardTitle>
        </Card>
	)
}

export default AddNoteCard
